import React from 'react';

const AccordionTitle = (({title, className}) => {
  
    return (
        
            <h2 className={className}>{title}</h2>
     
    );
});
                         
export default AccordionTitle;